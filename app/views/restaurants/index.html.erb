<div class="container">
  <h1>List of Restaurants</h1>
    <ul>
        <% @restaurants.each do |restaurant| %>
         <li> <%= restaurant.name %> <%= link_to "see details", restaurant_path(restaurant.id)  %></li>
        <% end %>
    </ul>
 <%= link_to "Add a restaurant", new_restaurant_path, class: "btn btn-primary"  %>
</div>
